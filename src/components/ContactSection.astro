<section class="contact">
    <div class="container">
        <h1>Επικοινωνήστε Μαζί μας</h1>
        <div class="contact-wrapper">
            <div class="contact-info">
                <p><strong>Διεύθυνση:</strong><br />Καυταντζόγλου 40, Αθήνα 111 44<br />δίπλα στον σταθμό “Άγιος Ελευθέριος”</p>
                <p><strong>Τηλέφωνα:</strong><br />2102289929<br />2130035849</p>
                <p><strong>Email:</strong><br /><a href="mailto:jgouthas@gmail.com">jgouthas@gmail.com</a></p>
                <p><strong>Ώρες Λειτουργίας:</strong><br />
                    Δευτέρα & Τετάρτη: 09:00 - 19:00<br />
                    Τρίτη, Πέμπτη, Παρασκευή: 09:00 - 20:00<br />
                    Σάββατο: Κλειστά
                </p>
            </div>

            <div class="contact-form">
                <p>Συμπληρώστε τη φόρμα και θα επικοινωνήσουμε μαζί σας το συντομότερο δυνατό.</p>
                <form name="contact" method="POST" data-netlify="true" netlify-honeypot="bot-field" action="/">
                    <input type="hidden" name="form-name" value="contact" />
                    <p hidden><label>Don’t fill this out: <input name="bot-field" /></label></p>

                    <div class="form-row">
                        <input type="text" name="firstName" placeholder="Όνομα" required />
                        <input type="text" name="lastName" placeholder="Επώνυμο" required />
                    </div>
                    <div class="form-row">
                        <input type="email" name="email" placeholder="Email" required />
                    </div>
                    <div class="form-row">
                        <textarea name="message" rows="5" placeholder="Μήνυμα" required></textarea>
                    </div>
                    <div class="form-row form-submit-row">
                        <label class="privacy-checkbox">
                            <input type="checkbox" required />
                            <span>Αποδέχομαι την <a href="/privacy-policy">Πολιτική Απορρήτου</a>.</span>
                        </label>
                        <button type="submit">Αποστολή</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>


<style>
    .contact {
        background: linear-gradient(to bottom, #000000, #121212);
        color: #fff;
        padding-left: 10%;
        padding-right: 10%;
    }

    h1 {
        text-align: center;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .contact-wrapper {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: stretch; /* ensures both columns have equal height */
    }

    .contact-info,
    .contact-form {
        background-color: rgba(255, 255, 255, 0.03);
        padding: 2rem;
        border-radius: 12px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        height: 100%;
    }

    .contact-form form {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .contact-form p,
    .contact-info p {
        font-size: 0.95rem;
        color: #ccc;
        margin-bottom: 1.5rem;
    }

    .form-row {
        display: flex;
        gap: 1rem;
        margin-bottom: 1rem;
    }

    .form-submit-row {
        justify-content: space-between;
        align-items: center;
        gap: 2rem;
        flex-wrap: wrap;
    }

    .privacy-checkbox {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        font-size: 0.85rem;
        color: #ccc;
        white-space: nowrap;
    }


    input,
    textarea {
        width: 100%;
        padding: 0.75rem;
        border-radius: 6px;
        border: 1px solid #333;
        background-color: #111;
        color: white;
        font-size: 0.95rem;
        box-sizing: border-box;
    }

    .privacy-checkbox {
        font-size: 0.85rem;
        color: #ccc;
        margin-top: 1rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .privacy-checkbox input[type="checkbox"] {
        accent-color: #00bfff;
        transform: scale(1.2);
    }


    button {
        background: #1e1e1e;
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        font-weight: bold;
        border-radius: 6px;
        cursor: pointer;
        transition: background 0.2s ease;
        align-self: flex-end;
        margin-top: 1rem;
    }

    button:hover {
        background: #333;
    }

    .map iframe {
        border-radius: 8px;
        width: 100%;
    }

    .map {
        margin: 3rem auto 0 auto;
        width: 50%;
    }

    /* Tablet responsiveness */
    @media (max-width: 1024px) {
        .contact {
            padding: 3rem 5%;
        }
    }

    /* Mobile responsiveness */
    @media (max-width: 768px) {
        .contact-wrapper {
            grid-template-columns: 1fr;
        }

        .form-row {
            flex-direction: column;
        }

        .form-submit-row {
            flex-direction: column;
            align-items: flex-start;
        }

        button {
            align-self: stretch;
            width: 100%;
        }

        .map {
            width: 100%;
        }
    }

    /* Very small devices */
    @media (max-width: 480px) {
        .contact {
            padding: 2rem 1rem;
        }

        .contact-form p,
        .contact-info p {
            font-size: 0.85rem;
        }

        input,
        textarea {
            font-size: 0.9rem;
        }
    }

</style>